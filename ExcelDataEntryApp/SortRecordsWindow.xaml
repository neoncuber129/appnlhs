<Window x:Class="ExcelDataEntryApp.SortRecordsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sắp xếp theo cột"
        Height="520"
        Width="520"
        MinHeight="320"
        MinWidth="460"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock FontWeight="SemiBold"
                   Text="Chọn cột và cách sắp xếp các dòng dữ liệu" />

        <Grid Grid.Row="1" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center"
                       Text="Cột sắp xếp:" />
            <ComboBox x:Name="SortColumnComboBox"
                      Grid.Column="1"
                      Margin="8,0,0,0"
                      DisplayMemberPath="Name"
                      MinHeight="32"
                      VerticalContentAlignment="Center"
                      SelectionChanged="SortColumnComboBox_OnSelectionChanged" />
        </Grid>

        <StackPanel Grid.Row="2" Margin="0,12,0,0">
            <TextBlock FontWeight="SemiBold"
                       Margin="0,0,0,6"
                       Text="Kiểu sắp xếp:" />
            <RadioButton x:Name="AscendingRadio"
                         Content="A → Z"
                         IsChecked="True"
                         Checked="SortModeRadio_OnChecked" />
            <RadioButton x:Name="DescendingRadio"
                         Margin="0,4,0,0"
                         Content="Z → A"
                         Checked="SortModeRadio_OnChecked" />
            <RadioButton x:Name="CustomRadio"
                         Margin="0,4,0,0"
                         Content="Tùy chỉnh"
                         Checked="SortModeRadio_OnChecked" />
        </StackPanel>

        <Border x:Name="CustomOrderPanel"
                Grid.Row="3"
                Margin="0,10,0,0"
                Padding="8"
                BorderBrush="#D1D5DB"
                BorderThickness="1"
                CornerRadius="4"
                Visibility="Collapsed">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock FontWeight="SemiBold"
                           Text="Thứ tự giá trị (dùng ▲ ▼ để sắp xếp)" />
                <ListBox x:Name="CustomValuesListBox"
                         Grid.Row="1"
                         Margin="0,8,0,0"
                         MinHeight="160">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,2,0,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock VerticalAlignment="Center"
                                           Text="{Binding Display}"
                                           TextTrimming="CharacterEllipsis" />
                                <Button Grid.Column="1"
                                        Width="32"
                                        Height="28"
                                        Margin="8,0,4,0"
                                        Padding="0"
                                        Click="MoveUp_Click"
                                        Content="▲" />
                                <Button Grid.Column="2"
                                        Width="32"
                                        Height="28"
                                        Padding="0"
                                        Click="MoveDown_Click"
                                        Content="▼" />
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <StackPanel Grid.Row="4"
                    Margin="0,12,0,0"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <Button Width="100"
                    Height="30"
                    Margin="0,0,8,0"
                    IsDefault="True"
                    Click="Sort_Click"
                    Content="Sắp xếp" />
            <Button Width="100"
                    Height="30"
                    IsCancel="True"
                    Content="Đóng" />
        </StackPanel>
    </Grid>
</Window>
